import { Injectable, signal } from "@angular/core";

@Injectable({ providedIn: "root" })
export class MochaDSElementsControllerService {
  toggleTheme() {
    // TODO: adapt from QML — references: currentTheme, currentTheme, currentTheme; QML-specific symbols
    // if(this.currentTheme==="Dark"){this.currentTheme="Light";switchTheme(santanderLight)}else{this.currentTheme="Dark";switchTheme(santanderDark)}
  }
}